services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5561:80"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/reader.db
      - PORT=5562
    extra_hosts:
      - "news.ycombinator.com:140.82.114.34"
    restart: unless-stopped

volumes:
  data:

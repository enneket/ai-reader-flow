services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "18562:5562"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/reader.db
      - PORT=5562
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "5561:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  data:

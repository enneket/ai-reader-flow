services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "18080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/reader.db
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "180:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  data:

# i18n Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English and Simplified Chinese language support with react-i18next, language selector in Settings page, auto-detection and localStorage persistence.

**Architecture:** Frontend-only i18n using react-i18next with JSON translation files. Language detection on first load, manual selection persisted in localStorage.

**Tech Stack:** react-i18next, localStorage

---

## File Structure

```
frontend/src/
├── i18n/
│   ├── index.ts          # i18n configuration + init
│   ├── en.json           # English translations
│   └── zh.json           # Chinese translations
├── main.tsx              # Wrap with I18nextProvider
└── components/
    ├── Layout.tsx        # Nav translations
    ├── FeedList.tsx      # Feeds page translations
    ├── ArticleList.tsx   # Articles page translations
    ├── NoteList.tsx      # Notes page translations
    └── Settings.tsx      # Settings page + language selector
```

---

## Tasks

### Task 1: Install react-i18next

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json`

- [ ] **Step 1: Install react-i18next**

Run: `cd /home/dabao/code/ai-flow/frontend && npm install i18next react-i18next`
Expected: package.json updated with i18next dependencies

- [ ] **Step 2: Commit**

```bash
git add package.json package-lock.json
git commit -m "deps: add react-i18next for i18n support

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 2: Create translation files

**Files:**
- Create: `frontend/src/i18n/index.ts`
- Create: `frontend/src/i18n/en.json`
- Create: `frontend/src/i18n/zh.json`

- [ ] **Step 1: Create `frontend/src/i18n/index.ts`**

```typescript
import i18n from 'i18next'
import {initReactI18next} from 'react-i18next'
import en from './en.json'
import zh from './zh.json'

const resources = {
  en: {translation: en},
  zh: {translation: zh},
}

// Get saved language or auto-detect
const getSavedLanguage = (): string => {
  const saved = localStorage.getItem('language')
  if (saved === 'en' || saved === 'zh') return saved

  const browserLang = navigator.language
  if (browserLang.startsWith('zh')) return 'zh'
  return 'en'
}

i18n
  .use(initReactI18next)
  .init({
    resources,
    lng: getSavedLanguage(),
    fallbackLng: 'en',
    interpolation: {
      escapeValue: false,
    },
  })

export default i18n

export const changeLanguage = (lang: 'en' | 'zh') => {
  localStorage.setItem('language', lang)
  i18n.changeLanguage(lang)
}
```

- [ ] **Step 2: Create `frontend/src/i18n/en.json`**

```json
{
  "nav": {
    "aiRss": "AI RSS",
    "feeds": "Feeds",
    "articles": "Articles",
    "notes": "Notes",
    "settings": "Settings"
  },
  "feeds": {
    "title": "RSS Feeds",
    "refreshAll": "Refresh All",
    "addFeed": "Add Feed",
    "placeholder": "Enter RSS feed URL",
    "viewArticles": "View Articles",
    "delete": "Delete",
    "empty": "No feeds yet. Add your first RSS feed to get started."
  },
  "articles": {
    "title": "Articles",
    "allFeeds": "All Feeds",
    "all": "All",
    "filtered": "Filtered",
    "saved": "Saved",
    "filterWithAI": "Filter with AI",
    "aiSummary": "AI Summary",
    "saveAsNote": "Save as Note",
    "empty": "No articles yet. Add a feed first."
  },
  "notes": {
    "title": "Notes",
    "note": "Note",
    "notes": "Notes",
    "empty": "No notes yet. Save articles to create notes.",
    "selectToView": "Select a note to view its content",
    "untitled": "Untitled Note"
  },
  "settings": {
    "title": "Settings",
    "aiConfig": "AI Provider Configuration",
    "provider": "Provider",
    "apiKey": "API Key",
    "baseURL": "Base URL",
    "model": "Model",
    "maxTokens": "Max Tokens",
    "saveAIConfig": "Save AI Config",
    "filterRules": "Filter Rules",
    "type": "Type",
    "value": "Value",
    "action": "Action",
    "addRule": "Add Rule",
    "language": "Language",
    "english": "English",
    "chinese": "中文"
  },
  "common": {
    "loading": "Loading...",
    "error": "Error",
    "success": "Success",
    "cancel": "Cancel",
    "confirm": "Confirm",
    "save": "Save",
    "delete": "Delete"
  }
}
```

- [ ] **Step 3: Create `frontend/src/i18n/zh.json`**

```json
{
  "nav": {
    "aiRss": "AI RSS",
    "feeds": "订阅源",
    "articles": "文章",
    "notes": "笔记",
    "settings": "设置"
  },
  "feeds": {
    "title": "RSS 订阅源",
    "refreshAll": "刷新全部",
    "addFeed": "添加订阅源",
    "placeholder": "输入 RSS 订阅源地址",
    "viewArticles": "查看文章",
    "delete": "删除",
    "empty": "暂无订阅源。添加第一个 RSS 订阅源开始使用。"
  },
  "articles": {
    "title": "文章",
    "allFeeds": "全部订阅源",
    "all": "全部",
    "filtered": "已过滤",
    "saved": "已收藏",
    "filterWithAI": "AI 筛选",
    "aiSummary": "AI 摘要",
    "saveAsNote": "保存为笔记",
    "empty": "暂无文章。请先添加订阅源。"
  },
  "notes": {
    "title": "笔记",
    "note": "笔记",
    "notes": "笔记",
    "empty": "暂无笔记。收藏文章来创建笔记。",
    "selectToView": "选择一个笔记查看内容",
    "untitled": "无标题笔记"
  },
  "settings": {
    "title": "设置",
    "aiConfig": "AI 服务商配置",
    "provider": "服务商",
    "apiKey": "API 密钥",
    "baseURL": "接口地址",
    "model": "模型",
    "maxTokens": "最大 Token 数",
    "saveAIConfig": "保存 AI 配置",
    "filterRules": "筛选规则",
    "type": "类型",
    "value": "值",
    "action": "动作",
    "addRule": "添加规则",
    "language": "语言",
    "english": "English",
    "chinese": "中文"
  },
  "common": {
    "loading": "加载中...",
    "error": "错误",
    "success": "成功",
    "cancel": "取消",
    "confirm": "确认",
    "save": "保存",
    "delete": "删除"
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/i18n/
git commit -m "feat: add i18n translation files (en, zh)

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 3: Integrate i18n into main.tsx

**Files:**
- Modify: `frontend/src/main.tsx`

- [ ] **Step 1: Update `frontend/src/main.tsx`**

```tsx
import React from 'react'
import {createRoot} from 'react-dom/client'
import {HashRouter} from 'react-router-dom'
import {I18nextProvider} from 'react-i18next'
import './App.css'
import App from './App'
import i18n from './i18n'

const container = document.getElementById('root')

const root = createRoot(container!)

root.render(
  <React.StrictMode>
    <I18nextProvider i18n={i18n}>
      <HashRouter>
        <App />
      </HashRouter>
    </I18nextProvider>
  </React.StrictMode>
)
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/main.tsx
git commit -m "feat: wrap app with I18nextProvider

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 4: Update Layout.tsx with translations

**Files:**
- Modify: `frontend/src/components/Layout.tsx`

- [ ] **Step 1: Update `frontend/src/components/Layout.tsx`**

```tsx
import {Link, useLocation} from 'react-router-dom'
import {Rss, FileText, Settings, LayoutGrid} from 'lucide-react'
import {useTranslation} from 'react-i18next'

interface LayoutProps {
  children: React.ReactNode
}

export function Layout({children}: LayoutProps) {
  const {t} = useTranslation()
  const location = useLocation()

  const isActive = (path: string) => {
    if (path === '/') return location.pathname === '/'
    return location.pathname.startsWith(path)
  }

  return (
    <div className="app">
      <aside className="sidebar">
        <div className="sidebar-header">
          <div className="sidebar-logo">
            <Rss size={24} />
            <span>{t('nav.aiRss')}</span>
          </div>
        </div>

        <nav className="sidebar-nav">
          <Link
            to="/"
            className={`nav-item ${isActive('/') && location.pathname === '/' ? 'active' : ''}`}
          >
            <LayoutGrid />
            <span>{t('nav.feeds')}</span>
          </Link>
          <Link
            to="/articles"
            className={`nav-item ${isActive('/articles') ? 'active' : ''}`}
          >
            <FileText />
            <span>{t('nav.articles')}</span>
          </Link>
          <Link
            to="/notes"
            className={`nav-item ${isActive('/notes') ? 'active' : ''}`}
          >
            <FileText />
            <span>{t('nav.notes')}</span>
          </Link>
          <Link
            to="/settings"
            className={`nav-item ${isActive('/settings') ? 'active' : ''}`}
          >
            <Settings />
            <span>{t('nav.settings')}</span>
          </Link>
        </nav>

        <div className="sidebar-footer">
          <div style={{fontSize: '12px', color: 'var(--text-muted)'}}>
            AI RSS Reader v1.0
          </div>
        </div>
      </aside>

      <main className="app-main">
        {children}
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/Layout.tsx
git commit -m "feat: add i18n to Layout component

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 5: Update FeedList.tsx with translations

**Files:**
- Modify: `frontend/src/components/FeedList.tsx`

- [ ] **Step 1: Read and update `frontend/src/components/FeedList.tsx`**

Add `useTranslation` from react-i18next and replace all hardcoded strings with `t('key')` calls:
- Title: `t('feeds.title')`
- Refresh All button: `t('feeds.refreshAll')`
- Add Feed input placeholder: `t('feeds.placeholder')`
- Add Feed button: `t('feeds.addFeed')`
- View Articles: `t('feeds.viewArticles')`
- Delete: `t('feeds.delete')`
- Empty state: `t('feeds.empty')`

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/FeedList.tsx
git commit -m "feat: add i18n to FeedList component

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 6: Update ArticleList.tsx with translations

**Files:**
- Modify: `frontend/src/components/ArticleList.tsx`

- [ ] **Step 1: Read and update `frontend/src/components/ArticleList.tsx`**

Add `useTranslation` and replace hardcoded strings:
- Title: `t('articles.title')`
- All Feeds: `t('articles.allFeeds')`
- All: `t('articles.all')`
- Filtered: `t('articles.filtered')`
- Saved: `t('articles.saved')`
- Filter with AI: `t('articles.filterWithAI')`
- AI Summary: `t('articles.aiSummary')`
- Save as Note: `t('articles.saveAsNote')`
- Empty state: `t('articles.empty')`

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/ArticleList.tsx
git commit -m "feat: add i18n to ArticleList component

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 7: Update NoteList.tsx with translations

**Files:**
- Modify: `frontend/src/components/NoteList.tsx`

- [ ] **Step 1: Read and update `frontend/src/components/NoteList.tsx`**

Add `useTranslation` and replace hardcoded strings:
- Title: `t('notes.title')`
- Note/Notes count: `t('notes.note')` / `t('notes.notes')`
- Empty state: `t('notes.empty')`
- Select to view: `t('notes.selectToView')`
- Untitled Note: `t('notes.untitled')`

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/NoteList.tsx
git commit -m "feat: add i18n to NoteList component

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 8: Update Settings.tsx with language selector

**Files:**
- Modify: `frontend/src/components/Settings.tsx`

- [ ] **Step 1: Read `frontend/src/components/Settings.tsx` to understand current structure**

- [ ] **Step 2: Update Settings.tsx**

Add `useTranslation` and import `changeLanguage` from i18n:
```typescript
import {useTranslation} from 'react-i18next'
import {changeLanguage} from '../i18n'
```

Add language selector section at top of settings content. Replace existing hardcoded strings with translation keys:
- Title: `t('settings.title')`
- AI Config section: `t('settings.aiConfig')`, `t('settings.provider')`, etc.
- Filter Rules section: `t('settings.filterRules')`, `t('settings.type')`, etc.
- Add language selector:
```tsx
<div className="settings-section">
  <h3>{t('settings.language')}</h3>
  <div className="form-group">
    <select
      value={i18n.language}
      onChange={(e) => changeLanguage(e.target.value as 'en' | 'zh')}
      className="form-select"
    >
      <option value="en">{t('settings.english')}</option>
      <option value="zh">{t('settings.chinese')}</option>
    </select>
  </div>
</div>
```

Add CSS for select if not already present.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Settings.tsx
git commit -m "feat: add language selector to Settings page

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```

---

### Task 9: Verify and test

**Files:**
- All modified files above

- [ ] **Step 1: Build the frontend**

Run: `cd /home/dabao/code/ai-flow/frontend && npm run build`
Expected: Build succeeds without errors

- [ ] **Step 2: Commit all remaining changes**

```bash
git add -A
git commit -m "feat: complete i18n implementation

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>"
```
